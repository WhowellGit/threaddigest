<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- The local staleness check: `insightminer doctor` with the stale-run alert (5 d, longer
	     than the Thursday-to-Monday gap of the twice-weekly schedule, D-30), every
	     hour at :15 (the run job fires at :30, so the two never start together). This file is a
	     template: deploy/launchd/install.sh replaces the root placeholder with the repository's
	     absolute path. (No double hyphen in this comment: XML forbids it.) -->
	<key>Label</key>
	<string>com.wesmax.insightminer.doctor</string>

	<key>ProgramArguments</key>
	<array>
		<string>/bin/bash</string>
		<string>__ROOT__/deploy/launchd/run.sh</string>
		<string>doctor</string>
	</array>

	<!-- A single dictionary with only Minute means "every hour at that minute". -->
	<key>StartCalendarInterval</key>
	<dict>
		<key>Minute</key>
		<integer>15</integer>
	</dict>

	<key>RunAtLoad</key>
	<false/>

	<key>WorkingDirectory</key>
	<string>__ROOT__</string>

	<key>StandardOutPath</key>
	<string>__ROOT__/data/logs/launchd-doctor.stdout.log</string>
	<key>StandardErrorPath</key>
	<string>__ROOT__/data/logs/launchd-doctor.stderr.log</string>

	<key>ProcessType</key>
	<string>Background</string>

	<key>ExitTimeOut</key>
	<integer>600</integer>

	<key>EnvironmentVariables</key>
	<dict>
		<key>PATH</key>
		<string>__ROOT__/.venv/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
		<key>LANG</key>
		<string>en_US.UTF-8</string>
	</dict>
</dict>
</plist>
