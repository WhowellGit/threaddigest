# Guards ledger

> Structure from the enforcement panel report (`docs/reference/reviews/2026-09-13-panel-enforcement.md` § F): **Active**, **Retired** (append-only), **Loosenings** (append-only, written by `make ratchet-loosen`, never by hand). Verdict vocabulary from the earlier project: **EARNED** (fired in anger and was right), **EARNED AT BIRTH ONLY**, **UNPROVEN**, **SELF-SERVING**. "Firings" are derived from CI failures posted to the pinned "Guard firings" issue, never typed here. Reviewed quarterly (`RUNBOOK.md` § 6). Adding a guard needs a birth incident, a positive control, a row here, and a check whether an existing guard can be widened; the guard-count ratchet makes an addition a loosening that pauses for approval. Two checks keep this ledger complete (2026-09-14): `tests/gates/test_known_issues_cite_collected_tests.py` goes red when a gate file under `tests/gates/` or a `@pytest.mark.gate("<ID>")` id has no row here, and the count of Active rows without a positive control is a ceiling in `.ratchets/review_only_rules.txt` (`guards_without_control`) that only goes down.
>
> Seeded 2026-09-13 with the M0 shipped gate set from the plan ("M0 foundation tranche") so the code agents can attach positive-control node ids. Every row is born clean (someone else's incident) and therefore **UNPROVEN**; the adversarial review's point stands that a guard never seen failing is a hypothesis. The `Positive control node` column is filled by the code agents with the pytest node id under `tests/gates/` (`@pytest.mark.gate("<ID>")`); a blank cell after M0 is a doc-currency failure. IDs follow the enforcement panel (G-nn) and, where the guard is a database spec, the database panel (DB-nn), so `TEST_STRATEGY.md`, the reports, and the markers agree.

## Active

| ID | Name | Birth incident | Mechanism | Positive control node | Fails how | Born | Verdict (date) | Firings → pinned issue |
|---|---|---|---|---|---|---|---|---|
| G04 | Layering (import-linter `layers` contract) | Plan § Module map ("layering enforced by import-linter, not convention"); earlier project's hot zones and god modules, `~/repos/insightminer-desktop-archive/Reddit/Database_Key_Learnings/SYSTEM_ARCHITECTURE_AND_REBUILD.md` via reviewer B | `.importlinter` layers `web \| cli` → `services` → `db \| adapters` → `ports` → `core`; `lint-imports` in `make check` and CI | `tests/gates/test_layering.py::test_gate_goes_red_when_a_contract_is_violated` | blocks merge | M0 | UNPROVEN (2026-09-13) | — |
| G05 | External-package chokepoints (`praw` only in `adapters.reddit_praw`; `sqlalchemy`/`alembic`/`sqlite3` only in `db/`; `fcntl` only in `services.lock`) | `docs/learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` rank 10 (opt-in guard bypassed by the main helper, A #44) | import-linter `forbidden` contracts; ruff `TID251` for `create_engine`, `sqlite3.connect`, `text(` outside `db/` (import-linter cannot ban symbols; adversarial D7) | `tests/gates/test_sqlite3_confined.py::test_the_scanner_catches_a_planted_import` (sqlite3-in-`db/` half); `tests/gates/test_layering.py::test_the_services_stdio_scanner_catches_a_planted_import` (the `typer`/`click`-out-of-`services/` half added by the round-5 P1 fix); no planted control yet for the `praw`-only-in-adapter or `fcntl`-only-in-`services.lock` halves | blocks merge | M0 | UNPROVEN (2026-09-13) | — |
| G06 | Network block in tests | Plan § Testing strategy principle ("the collector never touches the network in tests"); extended across the subprocess seam by adversarial B4 | `addopts = --block-network` (pytest-recording), `--record-mode=none` in CI; CLI refuses under `PYTEST_CURRENT_TEST` without `--gateway fake`; web routes use an injectable `ProcessRunner` | `tests/gates/test_pytest_config.py::test_socket_connect_raises_under_block_network` | test error / blocks merge | M0 | UNPROVEN (2026-09-13) | — |
| G15 | Schema snapshot golden (`db/schema.sql`) | `docs/learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` rank 12 and 15 (schema version literal copied into six producers; diverging migrations, B S2–S6) | `alembic upgrade head` on an empty DB must reproduce the committed normalized DDL plus the generated column-comment section; `make schema` regenerates; failure prints the diff (DB-01) | `tests/db/test_schema_golden.py::test_fingerprint_goes_red_when_the_live_schema_drifts` | blocks merge | M0 | UNPROVEN (2026-09-13) | — |
| G16 | Models == DDL | Same as G15 ("model edited without a migration" is a named AI-assisted failure pattern, plan § Known patterns) | pytest-alembic `test_model_definitions_match_ddl`, single head, upgrade, up-down consistency; `include_object` excludes `%_fts`, `%_fts_%`, `%_live` (DB-02, DB-03) | none yet (counted by the `guards_without_control` ceiling in `.ratchets/review_only_rules.txt`) | blocks merge | M0 | UNPROVEN (2026-09-13) | — |
| G19 | Data-directory isolation, in-process and across the subprocess seam | `docs/learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` rank 1 (tests overwrote live artifacts twice; reads honored the test dir, writes did not, A #53–54); adversarial B4 (the same incident one process boundary over) | autouse fixture sets `INSIGHTMINER_DATA_DIR=tmp_path`; `Settings` raises when pytest is loaded and the dir resolves to the default unless `INSIGHTMINER_ALLOW_REAL_DATA_DIR=1`; data dir passed to children through the environment; `Path.home`/`expanduser` banned outside `settings.py` (DB-18, DB-19) | `tests/gates/test_data_dir_isolation.py::test_refuses_default_data_dir_while_pytest_is_loaded`, `::test_refuses_when_env_points_at_the_default_dir`, `::test_child_process_under_pytest_current_test_refuses` | test error | M0 | UNPROVEN (2026-09-13) | — |
| G31 | Connection chokepoint, behavioral pragma test | `docs/learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` rank 10 (freshness guard written but never wired; an inert WAL guard on a read-only connection, A #17, #44) | a pooled connection obtained through `db.engine` on a read-write file DB reports `journal_mode=wal`, `foreign_keys=1`, `secure_delete=1`, `busy_timeout=30000`, `synchronous=1`, `temp_store=2` (DB-12) | `tests/db/test_engine_pragmas.py::test_two_pool_checkouts_both_carry_the_pragmas` | blocks merge | M0 | UNPROVEN (2026-09-13) | — |
| G34 | Retired claims not stated as live (`tests/gates/test_superseded_claims.py`) | 2026-09-13: the plan's gates table listed eight retired gates as live, contradicting `DECISIONS.md`; the earlier project's number-one drift bug reproduced here. Widened 2026-09-15 (plan-version-two review): the retired daily schedule was alive in `core/retry.py` as a constant, a deferral rule, and a comment citing the plan, and in a wrapper header, because the scan covered documents only (KI-025) | phrases in `DECISIONS.md` § Retired claims; any line of a live document, a `src/` module, or a `deploy/` file stating one without a retirement marker is red; cited decision ids must exist | `tests/gates/test_superseded_claims.py::test_positive_control_stale_claim_is_red_and_annotated_claim_is_green`, `::test_positive_control_a_retired_phrase_in_source_is_red`, `::test_positive_control_unknown_decision_id_is_red` | test fails naming file:line | 2026-09-13 | SEEN RED 2026-09-13 (first run flagged stale rows in the applied-learnings doc and the plan's deployment table) | 0 |
| G35 | No imported identifiers in tracked text (`tests/gates/test_no_imported_identifiers.py`) | 2026-09-13: the rules against importing the earlier project's identifiers and against attribution trailers were kept by memory alone until a sweep rewrote them (measured 2026-09-14 from the pre-sweep tree with the gate's rules: 85 occurrences in 12 files); widened 2026-09-14 after a lowercase codename survived inside a quoted transcript line and the company's name survived inside an imported lint name | scans every tracked file whose extension is `.md .py .toml .yaml .yml .txt .json .cfg .ini .sh .mako` for nine rules (attribution trailers; the main session's model name; e-mail addresses; ticket keys; IPv4 addresses; Slack-style channel ids; a home path belonging to neither the operator nor the `[user]` placeholder; the earlier project's four codename tokens; its register-id shapes); narrow carve-outs for loopback/unspecified addresses and RFC 2606/6761 reserved domains plus the project's own recorded git identity; one excluded path, `docs/reference/earlier-project-retrospectives/` | `tests/gates/test_no_imported_identifiers.py::test_positive_control_a_planted_identifier_is_red`, `::test_positive_control_the_same_tree_without_the_violation_is_green`, `::test_positive_control_the_carve_outs_are_narrow` | test fails naming file:line for each hit | 2026-09-13 | SEEN RED 2026-09-13 (85 violations across 12 files on the pre-sweep tree) | 0 |
| G36 | Attribution trailers refused at commit time (`tools/hooks/no_bypass_git.sh`) | 2026-09-13: attribution trailers were refused by written policy only, never mechanically, until this hook landed alongside G35's identifier sweep | `no_bypass_git.sh` refuses a `git commit` whose command text, or whose `-F`/`--file` message file, carries an attribution trailer; a message file that cannot be read, and `-F -`, block as well (fails closed) | `tests/gates/test_hooks.py::test_positive_control_a_trailer_in_a_message_file_is_red` | hook exits 2, BLOCKED, naming "attribution trailer" | 2026-09-13 | SEEN RED 2026-09-13 (via its positive control) | 0 |
| G39 | Rules name an enforcer that exists (`tests/gates/test_rules_name_their_enforcer.py`) | 2026-09-13: five of fourteen rows in `CLAUDE.md`'s "Rules and what enforces them" table named their enforcer only in prose | resolves every backticked path, pytest marker, ruff code and `make` target in the rule table's "Enforced by" cell against the tree; a row with no resolving enforcer must say "review"; the review-only count is a ceiling in `.ratchets/review_only_rules.txt` that only goes down | `tests/gates/test_rules_name_their_enforcer.py::test_positive_control_an_enforcer_that_does_not_exist_is_red`, `::test_positive_control_a_review_row_is_counted_not_failed` | test fails naming the row | 2026-09-13 | SEEN RED 2026-09-13 (five prose-only rows) | 0 |
| G40 | Register node ids resolve (`tests/gates/test_known_issues_cite_collected_tests.py`) | 2026-09-13: `KNOWN_ISSUES.md` and `GUARDS.md` could cite a regression test or positive control that no longer existed and still read as enforced | resolves every node id in `KNOWN_ISSUES.md`'s regression-test column and `GUARDS.md`'s positive-control column structurally with `ast` (no pytest collection subprocess): `path::function` and `path::Class::method` must be defined in the file named; `::name` continues the previous path in the same cell; HTML comments are stripped so the format example stays documentation | `tests/gates/test_known_issues_cite_collected_tests.py::test_positive_control_a_dangling_node_id_is_red`, `::test_positive_control_a_second_control_from_the_same_file_resolves` | test fails naming the dangling node id | 2026-09-13 | UNPROVEN (2026-09-13; both registers resolved on first run) | 0 |
| G23 | Hard-block hooks: no `--no-verify`, push, or unchecked merge into `main`; no hand edits to `.ratchets/` or the hook settings (`tools/hooks/`) | Plan § Gates (two hooks, N-16); the earlier project's write guard was bypassed by the main helper; external round one (2026-09-14) showed every non-fast-forward merge form and seven shell-indirection forms passing (KI-019, KI-020), and its review panel (2026-09-15) found a backslash-newline line continuation, a separated `--config-env core.hooksPath`, a doubled slash in `.claude//settings.json`, and case-variant paths on the case-insensitive filesystem all slipping past | the `PreToolUse` command hooks in `.claude/settings.json`, self-protecting, failing closed on internal error; since 2026-09-14 a merge into `main` (and `git pull` there) is refused unless `.build/check-green.json`, written by `tools/check_stamp.py` as the last step of `make check` from the staged tree, names the tree being merged, the merge is `--ff-only`, and `main` is an ancestor of the branch; variables, `-c` strings, `eval` and `xargs` are resolved, unresolvable command words and interpreter one-liners that name git are refused, and a variable used where a protected path could be written is refused; the tokenizer joins backslash-newline continuations and strips comments and redirections before parsing, protected-path matching is slash-tolerant and case-insensitive, and a hooksPath override is caught in the `--config-env` form as well as `-c` (round-one panel, 2026-09-15). **What these hooks are:** mistake prevention for the agent, in the path of every tool call; not a security boundary. A script file the agent writes and then runs is outside their sight by design; the boundary against a deliberate bypass is pre-commit, CI once a remote exists, review, and the remote's branch protection (Wes's ruling, 2026-09-14) | `tests/gates/test_hooks.py::test_no_bypass_git_decides_bash_commands`, `::test_merge_into_main_needs_the_green_stamp_for_that_tree`, `::test_check_stamp_names_the_staged_tree_and_refuses_untracked_files`, `::test_enforcement_files_decides_file_tools`, `::test_hooks_fail_closed_on_malformed_input`, `::test_settings_json_registers_every_hook_script_with_timeout_5` | tool call blocked | M0 | UNPROVEN (2026-09-14; a hook loads only in a session started in the repo root, so it has not been seen red in anger) | 0 |
| G30/GT-01 | Guard reachability: every post-run invariant planted and observed through `insightminer run` | Plan § Gates (the earlier project shipped a gate that was unit-tested and called by nothing) | `tests/gates/test_invariants_planted.py` drives the CLI with a planted violation per invariant and asserts `runs.status` flips | `tests/gates/test_invariants_planted.py::test_planted_violation_flips_the_run`, `::test_every_invariant_has_a_planter`, `::test_a_crashing_invariant_fails_the_run_rather_than_leaving_it_running` | run `failed` | M1a | UNPROVEN (2026-09-14) | 0 |
| G33 | Doc currency: `docs/INDEX.md` and `docs/**` agree in both directions | Plan § Gates (reduced 2026-09-13 to the INDEX check) | `tests/gates/test_doc_currency.py` | `tests/gates/test_doc_currency.py::test_positive_control_orphan_ghost_and_duplicate` | CI fails | M0 | UNPROVEN (2026-09-14) | 0 |
| CF-01/CF-02 | No bypass: a dry run writes nothing, the budget is clamped to the hard cap, `--no-comments` records a reason, the fake gateway refuses the default data directory | Plan § Gates (a design rule since N-06) | `tests/gates/test_no_bypass.py` through the CLI | `tests/gates/test_no_bypass.py::test_dry_run_writes_nothing_anywhere`, `::test_budget_is_clamped_to_the_hard_cap`, `::test_fake_gateway_refused_against_the_default_data_dir` | test fails | M1a | UNPROVEN (2026-09-14) | 0 |
| DB-54 | Run counters equal table deltas | Plan § Silent-failure controls (a partial run reported as success) | post-run invariant `counters_equal_table_deltas`, proven through the CLI | `tests/gates/test_counters_deltas.py::test_planted_extra_row_fails_the_run` | run `failed` | M1a | UNPROVEN (2026-09-14) | 0 |
| RL-04 | Every mutating command takes the collector lock and writes a run row | Plan § Collector algorithm step 0 | `tests/gates/test_mutating_commands.py` walks the Typer command tree | `tests/gates/test_mutating_commands.py::test_every_mutating_command_takes_the_lock_and_writes_a_run_row`, `::test_excluded_commands_write_nothing` | test fails | M1a | UNPROVEN (2026-09-14) | 0 |
| G44 | Routing pointers resolve (`tests/gates/test_routing_rows_resolve.py`) | 2026-09-14: the routing tables and rule files pointed at documents and sections nothing checked; the first run found a section cited by a name the runbook does not use and rule files naming package-relative paths | every backticked path in the three routing tables, the rule files, and the memory snapshot exists; every `§` reference matches a heading, including every quoted target after an arrow (widened 2026-09-15: a router row's second quoted target named a section the plan's version two had retired and only the first was checked); every rule file's `paths:` globs match a tracked file | `tests/gates/test_routing_rows_resolve.py::test_positive_control_dangling_file_section_and_code_are_red`, `::test_positive_control_a_rule_file_matching_nothing_is_red` | test fails naming the pointer | 2026-09-14 | EARNED AT BIRTH ONLY (2026-09-14; two real dangling pointers on the first run) | 0 |
| G45 | Status-page contract (`tests/gates/test_status_page.py`) | 2026-09-14: the status page was stale within a day (merged work listed as in flight; three numbers wrong) while the decision that capped it sat unenforced | line cap; `**As of` stamp within two days of HEAD; four required headings; no restated counts | `tests/gates/test_status_page.py::test_positive_control_each_failure_is_named` | test fails naming the line | 2026-09-14 | EARNED AT BIRTH ONLY (2026-09-14) | 0 |
| G46 | No sleeping under pytest (`tests/gates/test_no_sleep_under_pytest.py`) | Plan § Testing strategy (tests control time through the injected clock and never sleep) | the clock the CLI builds under pytest raises on sleep | `tests/gates/test_no_sleep_under_pytest.py::test_guard_clock_sleep_raises` | test fails | M1a | UNPROVEN (2026-09-14) | 0 |
| G47 | Memory snapshot: add-or-update only, restore asserts completeness, one memory home, snapshot never behind live (`tools/memory_snapshot.py`) | the earlier project's memory export mirrored deletions and its restore only counted; 2026-09-14: a second memory home had diverged by 589 bytes | `make check` runs `check` (index, reachability, frontmatter, second homes) and `diff` (a snapshot behind live memory is red) | `tests/gates/test_memory_snapshot.py::test_a_deletion_in_live_memory_never_deletes_from_the_snapshot`, `::test_restore_is_red_when_a_destination_byte_differs`, `::test_check_reports_a_second_memory_home_with_topic_files` | `make check` fails | 2026-09-13 | EARNED AT BIRTH ONLY (2026-09-14; the second-home check found the Desktop-keyed copy) | 0 |
| G48 | No tracked type-checker daemon state (`tests/gates/test_no_tracked_daemon_state.py`) | KI-001 (a fresh clone type-checked the original tree) | `.dmypy.json` untracked and ignored; every invocation uses the build-directory status file | `tests/gates/test_no_tracked_daemon_state.py::test_dmypy_state_is_not_tracked_and_is_ignored`, `::test_dmypy_invocations_use_a_build_dir_status_file` | test fails | 2026-09-13 | EARNED AT BIRTH ONLY (2026-09-13) | 0 |
| G49 | Read before touch, the third hook (`tools/hooks/read_before_touch.sh`) | Wes, 2026-09-14: routing rows were written and never mechanically followed; the routing gate proves the pointers exist, and this hook makes reading them the price of editing what they govern (N-16's two-hook cut amended for it) | on Edit, Write, or MultiEdit under a rule file's `paths:`, the documents that rule file names first must appear in the session transcript (scanned incrementally); mode in `tools/hooks/read_before_touch.mode`: `log` appends to `.build/hooks/read_before_touch.jsonl` and allows, `block` exits 2; registered in the hook settings by a human only | `tests/gates/test_hooks.py::test_read_before_touch_blocks_in_block_mode_and_allows_once_read`, `::test_read_before_touch_logs_and_allows_in_log_mode`, `::test_read_before_touch_internal_errors_follow_the_mode`, `::test_settings_json_registers_every_hook_script_with_timeout_5` (registered, or red) | tool call blocked (block mode); ledger line (log mode) | 2026-09-14 | UNPROVEN (2026-09-14; log-first, ledger review 2026-09-28; registration gated by the settings test, red until pasted) | 0 |
| G51 | Code-health ratchet family (`tools/code_health.py` → `.ratchets/code_health.txt`) | Wes, 2026-09-14: at a milestone maintainability passes on its own, judged by deterministic analysis and never by asking a model whether the code is clean; the earlier project's god functions and 5,359-line script (plan § Size ratchets) | `make check` runs complexipy, radon, ruff's size rules, vulture with a counted whitelist (decorator-registered names ignored), and pylint's duplicate-code over the tree and writes one report; `tools/ratchet.py` holds each count as a ceiling (down, slack 0): functions over cognitive 15, functions over cyclomatic 15, modules below maintainability rank A, size-rule violations, dead code, whitelist entries, duplicate blocks; a missing report is red, never zero; birth relaxations carry `hard_after` dates that bump clears once a count reaches zero | `tests/gates/test_code_health.py::test_planted_offenders_are_counted_and_named`, `::test_a_missing_analyzer_is_red_not_zero`, `tests/gates/test_ratchet.py::test_compare_is_red_when_a_code_health_count_rises`, `::test_bump_clears_a_hard_after_once_a_ceiling_reaches_zero` | blocks merge (loosening → approval; expiry → red) | 2026-09-14 | UNPROVEN (2026-09-14) | — |
| G52 | Review packet from the committed tree only (`tools/review_packet.py`) | Wes, 2026-09-14: the plan's rule that nothing but the clean tree or a diff ever leaves this machine was written, never mechanical, while the earlier project's material sits beside this one on the same disk | allowlisted tracked paths read from `HEAD` with `git show`, never the working copy; a dirty tree is refused unless `--allow-dirty`, which stamps the manifest and README; secrets, data, fixtures, the earlier project's folders, the memory snapshot, and dated review reports are excluded by prefix; a sha256 per file and one packet hash in `MANIFEST.json`; the packet from `HEAD` is scanned by the identifier gate's own rules | `tests/gates/test_review_packet.py::test_a_packet_holds_only_the_allowlist_from_head`, `::test_a_dirty_tree_is_refused`, `::test_this_repositorys_packet_carries_no_imported_identifier` | build refused (dirty tree); test red (a leak or a stale allowlist) | 2026-09-14 | UNPROVEN (2026-09-14) | — |
| G53 | Behavioural dependency pins keep their bound (`tests/gates/test_dependency_pins.py`) | External round one, test-methodology seat (2026-09-15): the `praw>=8,<9` pin (reviewer C-11) shipped with no test, so an accidental loosening back to `praw>=7.8` -- the exact regression it fixed, where `replace_more` discovers 20 comments not 100 and breaks the request budget -- was invisible to the suite | reads the invariant (`pyproject.toml` `[project].dependencies`): the `praw` requirement must exclude PRAW 9 and any 7.x while admitting the tested 8.x, and the installed/resolved version must be 8.x and satisfy the spec | `tests/gates/test_dependency_pins.py::test_praw_is_pinned_to_the_tested_major`, `::test_installed_praw_satisfies_the_pin` | blocks merge | 2026-09-15 | UNPROVEN (2026-09-15) | — |
| G54 | Harness page inventory derived from the tree (`tools/harness_page.py`, `docs/INSIGHTMINER_HARNESS.md`) | Documentation-practices assessment (2026-09-13) and Wes's ask for this project's own harness document (2026-09-15): the earlier project's hand-written harness document was never opened after it was written and drifted to naming a mechanism deleted weeks earlier. Widening considered: G33 checks the router against the tree and G47 the memory snapshot against live memory; neither reads the mechanism files, so this is a new artifact class rather than a widened rule | the block between the page's markers is rendered from the hook scripts and their registration, the pre-commit configuration, the gate files, the ledger's row counts, the ratchet keys, the import-linter contracts, the rule files, the skills, the tools, the `make` targets, and which gate reads each register; the gate compares the committed block with a fresh render and fails with the diff; `--write` regenerates it | `tests/gates/test_harness_page.py::test_positive_control_a_new_hook_script_makes_the_page_stale`, `::test_positive_control_missing_page_or_markers_is_red` | test fails with the diff | 2026-09-15 | UNPROVEN (2026-09-15) | — |
| G50 | Review register (`tests/gates/test_review_register.py`) | 2026-09-14: the harness protocol said every review run is recorded; the design panels were, the code panel that closed 24 findings was not, and 33 of 62 commits touched review-required surfaces with no record | every commit on `main` since 2026-09-15 touching a review-required surface is covered by a row's scope in `docs/reference/reviews/REGISTER.md`; every record cited exists | `tests/gates/test_review_register.py::test_positive_control_an_uncovered_surface_commit_is_red`, `::test_positive_control_a_missing_record_is_red` | test fails naming the commit | 2026-09-14 | UNPROVEN (2026-09-14) | 0 |
| DB-20/GT-02 | Upsert semantics and PK stability | `docs/learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` rank 3 (`INSERT OR REPLACE` burned 16,813 rowids and raised a false divergence alarm, A #18); adversarial A10 (`AUTOINCREMENT`, drop `max(pk)==count(*)` at runtime) | `INSERT … ON CONFLICT(reddit_id) DO UPDATE` only; `AUTOINCREMENT` on `posts`/`comments`; three fake runs leave `pk` and `first_seen_at` unchanged per `reddit_id`; duplicate-in-page yields one row; purges recorded on the run row (DB-55) | `tests/gates/test_pk_stability.py::test_three_reruns_keep_pk_and_first_seen_at`; positive control for the old failure mode `tests/db/test_alembic.py::test_insert_or_replace_burns_the_pk_and_detaches_fts` | blocks merge (M1a: run `failed`) | M0 (repo) / M1a (run) | UNPROVEN (2026-09-13) | — |
| G11 | Coverage ratchet | Plan § Gates; enforcement panel § C (hard floor with half a point of slack; a stale floor is red) | coverage json over `core`, `services`, `adapters/reddit_fake/` vs `.ratchets/coverage.txt`; three-way compare (measured vs file, file vs `render(measured)`, file vs `main`) by `tools/ratchet.py`; direction up | `tests/gates/test_ratchet.py::test_compare_is_red_when_coverage_falls_below_the_floor` | blocks merge (loosening → approval) | M0 | UNPROVEN (2026-09-13) | — |
| G10 | Suppression ratchet (`noqa`, `type: ignore`, `no cover`, `filterwarnings ignore`, mypy overrides) | `docs/learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` § 2 rule 8 and `docs/reference/earlier-project-retrospectives/WHY_THE_GUARDS_EXIST.md` (2,250 grandfathered suppressions became a permanent exemption) | `tools/ratchet.py measure`; ceiling 0 per key; every hit printed with file:line on every run; each needs a rule or error code; mypy `praw.*` override declared with a reason and counted | `tests/gates/test_ratchet.py::test_compare_is_red_when_a_skip_or_suppression_is_added` | blocks merge (loosening → approval) | M0 | UNPROVEN (2026-09-13) | — |
| G08/G09 | Skip/xfail ratchet at zero and runtime skipped == 0 | Plan § Gates; adversarial A13 (`-m "not live"` in `addopts` so opt-in suites are deselected, never skipped) | AST count of skip constructs with `reason="#n …"`; junit `skipped` of the required selection must be 0; `tests/live` fails without credentials when explicitly selected | `tests/gates/test_ratchet.py::test_compare_is_red_when_a_skip_or_suppression_is_added` | blocks merge (loosening → approval) | M0 | UNPROVEN (2026-09-13) | — |
| G01 | Exception-policy lint | Plan § Silent-failure controls; `docs/reference/reviews/2026-09-12-db-learnings-review-A.md` § A (silent-failure swallowing in KEY_LEARNINGS) | ruff `E722 BLE001 S110 S112 B904 TRY2xx TRY3xx RUF100`; every handler re-raises or records a warning counter on the run row; any warning makes the run `partial` | none yet (counted by the `guards_without_control` ceiling in `.ratchets/review_only_rules.txt`) | blocks commit and merge | M0 | UNPROVEN (2026-09-13) | — |
| G07 | Warnings are errors in tests | Plan § Silent-failure controls (PRAW and SQLAlchemy deprecations must surface, not rot) | `filterwarnings = error`; proven by running pytest the way CI runs it | `tests/gates/test_pytest_config.py::test_warnings_are_errors_inside_tests` | blocks merge | M0 | UNPROVEN (2026-09-13) | — |
| G29 | Pre-commit: ruff format + lint, `dmypy` whole tree, gitleaks, large-file check, `no-commit-to-branch main` | `docs/learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` rank 8 ("the only thing between a regression and the corpus was someone remembering to run the full suite", B T1) | `.pre-commit-config.yaml` via `uv run` so versions come from `uv.lock`; `make setup` installs the hooks; the weekly portability job commits a fake key and a 2 MB file and expects both rejected | external control: pre-commit refused the first hook-gated commit on 2026-09-14 (end-of-file-fixer), so it has been seen red in anger; no in-suite control can plant a commit | blocks commit | M0 | EARNED (2026-09-14) | — |

Notes: the ratchet guards (G08–G11) gained an optional `hard_after` expiry on relaxed lines on 2026-09-13 (`tools/ratchet.py`; controls in `tests/gates/test_ratchet.py`): a relaxed ratchet line may now carry an expiry date, printed as RELAXED on every run while live and turning the comparison red (EXPIRED, naming the metric and the way back) the day after it passes; `bump` carries the date over untouched, and only `tools/ratchet.py` (via `make ratchet-loosen ... HARD_AFTER=YYYY-MM-DD`) may write one. Since 2026-09-14 `bump` also clears a date once a ceiling has reached zero, so a solved relaxation cannot go red on its expiry day (control `tests/gates/test_ratchet.py::test_bump_clears_a_hard_after_once_a_ceiling_reaches_zero`); the code-health family G51 was born with three dated relaxations, written by `make ratchet-loosen` into the Loosenings table below.

## External controls (no positive control possible; a dated "last seen red" line instead, per the adversarial split)

| Control | Where | Last seen red | Notes |
|---|---|---|---|
| Branch protection / required checks on `main` | GitHub settings | never (not yet created) | Depends on the GitHub plan and the agent's token scope (`DECISIONS.md` § 7). Until settled, CI is a gate with a git trail, not an enforcement authority |
| Hard-block hooks (three since 2026-09-14: the two hard blocks and read-before-touch, which is log-first and was registered by Wes on 2026-09-14): no `--no-verify` or direct push to `main`; no hand edits to `.ratchets/` or the hook settings | `.claude/settings.json` → `tools/hooks/` | never | Self-protecting, fail closed on internal error; govern only Claude Code tool calls in this project. Note (2026-09-13): project hook settings load only from the session's starting directory, so "never seen red" is not evidence the hook is live; sessions start in `~/repos/insightminer`. Note (2026-09-13): the no-`--no-verify` hook (`tools/hooks/no_bypass_git.sh`) now also refuses a commit whose message or `-F`/`--file` carries an attribution trailer, proven by its own positive control (G36) rather than by this row's "never seen red" |
| Pre-push `make check` | `.git/hooks/pre-push`, a pre-commit stage installed by `make hooks` (`.pre-commit-config.yaml`) | never (installed 2026-09-14; no remote yet) | A remote is a backup, never the gate; `doctor` and the hooks line of `make check` report both stages installed |
| Portability job | CI weekly and on quickstart-file changes | never | Fresh Ubuntu container: `git clone && make setup && make check` |
| Healthchecks.io ping (M1d) | end of `run` | never | Replaces the launchd dead-man |

## Planned at M1a (post-run invariants; proven through `insightminer run --gateway fake` with a planted violation)

The M1a invariants that shipped now sit in § Active (DB-54, G30/GT-01, RL-04, CF-01/CF-02); the compliance canary (DB-32) arrives with M1c, and structural population floors (DB-50, DB-51) and per-source freshness (ingest FR-01) start as `partial`/amber for the first 60 days, as the plan says.

## Retired

| ID | Date | Verdict | Reason | Replaced by |
|---|---|---|---|---|
| | | | | |

## Loosenings

| Date | Key | From | To | Reason | PR |
|---|---|---|---|---|---|
| 2026-09-14 | code_health.cognitive_over_15 | 8 | 8 | birth relaxation 2026-09-14: eight functions over cognitive 15 (fetch_page, normalize_ws, three in the fake gateway, two in themes, _page_loop); zero by the end of M1d or re-approved with a reason hard_after=2026-09-27 |  |
| 2026-09-14 | code_health.cyclomatic_over_15 | 1 | 1 | birth relaxation 2026-09-14: one fake-gateway method at cyclomatic 21; zero by the end of M1d or re-approved with a reason hard_after=2026-09-27 |  |
| 2026-09-14 | code_health.mi_below_a | 1 | 1 | birth relaxation 2026-09-14: the fake gateway module at maintainability rank C; its split is in flight and bump clears the date once the count is zero hard_after=2026-09-27 |  |
