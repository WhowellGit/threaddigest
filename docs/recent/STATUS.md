---
purpose: What is in flight, what is next, what must not be undone, and what waits on Wes; the resume surface.
update-policy: rewritten
mirrors: [docs/decisions/DECISIONS.md, docs/PLAN.md, docs/runbook/KNOWN_ISSUES.md]
milestone: M1a-A
---
# STATUS (rewritten whole, never appended; contract enforced by `tests/gates/test_status_page.py`; its `milestone` is the clock the document contract reads)

**As of 2026-09-16.** M0 and M1a tranche A are built and green on `main`; the three hooks are registered; the round-one fixes, the twice-weekly cadence (D-30), KI-011, and the round-one fix panel's follow-ups have landed. The plan is at version 2 (`docs/PLAN.md`, rewritten 2026-09-15), with `docs/OVERVIEW.md` and `docs/INSIGHTMINER_HARNESS.md` beside it. No remote yet: a bare git remote on the QNAP is the first backup (MB), GitHub later. Numbers live in the `make check` output, never here.

## In flight
- The wider review of plan version two, which Wes asked for on 2026-09-16: whether version one's insights survived the rewrite, whether the rewrite and its changes were scrutinised as deeply as version one's rounds, and whether the hooks, the harness, and the testing methodology are aligned to start tranche B. Runs as a review round with focused seats; its record lands under `docs/reference/reviews/` and its findings here.
- Design-to-build reconciliation, landed 2026-09-16: the plan, D-25, the Postgres-exit section, and the runbook named a search-index port and `VACUUM INTO` backups the database layer never had (both retired as claims); corrected in place, the reasons recorded in the decisions log, retired-claims rows added, and G55 widened to resolve every code identifier a rewritten document names (class-like names a ceiling in `.ratchets/docs.txt`); G34 reads through backticks. Record: `docs/decisions/DECISIONS.md` § 2026-09-16; `docs/learnings/DOC_DRIFT_FINDINGS_2026-09-13.md` rows 23 and 24.
- Plan version two landed on 2026-09-15 with the overview, the harness page and its generated inventory (G54), three cadence residues fixed with failing tests first (KI-024 to KI-026), the retired-claims gate widened to source and deploy files, and the decisions of that day (D-31, D-32, Premiere first with workspaces as the designed option, the high-volume levers deferred, D-13 closed). Record: `docs/reference/reviews/2026-09-15-plan-v2-review.md`.
- The document and memory process is mechanized: front-matter contracts checked by `tools/doc_policy.py` in `make check` (G55), append-only documents held to their diff, dated annotations a ratchet ceiling, milestone lag bounded, a live-facts table checked against every mirror, memory topic files capped. Procedure: `docs/runbook/RUNBOOK.md` § 8 and the `docs-sweep` skill. Record: `docs/reference/reviews/2026-09-15-doc-policy-review.md`.
- The read-before-touch hook is registered log-first; its ledger review is on 2026-09-28. The code-health birth relaxations expire 2026-09-27 unless the counts reach zero or Wes re-approves.
- Hardening queue, compliance first (details in `docs/reference/reviews/2026-09-14-testing-and-workflow-panel.md` and `docs/reference/reviews/2026-09-15-round-one-fix-panel.md`): the between-run controls before the schedule goes live (dead-man ping, scheduled backups with a restore command and one drill, retention pruning per D-31, a free-disk precondition on `run`, signal handling); the migration-checklist tests before the next migration on posts or comments (KI-014); the storage-level canary matrix at M1c; the rev-2 design items for M2 (manual tags' nullable rule key, a cancel column, restore and import gate text naming curation, import over a diverged database, theme delete and merge behind the typed gate, the operator-complete gate reaching UI-only actions, identity coverage and tree completeness beside every ranked count with a golden, which curation records survive a failed restore); the methodology items (a `make mutate` target at M3, a reading-week record with a `doctor` age check, the three declared-but-unused test tools, the batch-migration key-recycling test, the wider data-directory write test).

## Next
1. Tranche B when credentials and the API access approval arrive: the real Reddit adapter probe-first (`probe --save-fixture`), cassettes, the `doctor` auth ping; one credentialed probe day capturing the shapes the deletion predicates and the fake's summary assume (KI-023's fidelity limit included) precedes M1b.
2. M1b (comment trees), M1c (deletion compliance), M1d (themes, digest, the schedule, the seven-digest reading week): brief → bounded design rounds → build on a branch → panel. The workspace-aware run loop stays out of M1d (Wes, 2026-09-15).
3. QNAP bare remote when the hardware arrives (MB): add the remote, first push through the pre-push hook, nightly `git bundle` to the same share.
4. The review runner and the second external round at tranche B's design freeze; the fixes and the M1b/M1c designs are that round's job, with a code-executing seat.
5. Code-health layer two: the CodeScene trial after the first milestone past M1d (`docs/decisions/DECISIONS.md` § 2026-09-14 (later)).
6. Moving the three earlier-project assessments beside `docs/reference/earlier-project/EARLIER_PROJECT_REFERENCE.md` as appendices (every citation of their paths moves with them).
7. At the M1d retrospective: score the predictions in `docs/learnings/LEARNINGS_TRANSFER.md` §5 and §7 (the external round's yield; version two needing no inline annotation), and rule on the argument-count ceiling.

## Do not undo
- Local history has been rewritten three times (retrospectives, trailers, identifiers); never restore a bundle into `main` without re-running the whole-history identifier scan, and never push a bundle's history anywhere.
- The memory home is the repo-keyed directory; the Desktop-keyed one holds a pointer only, and `make check` goes red if a second home grows topic files again.
- Sessions start in `~/repos/insightminer`; a session started elsewhere runs without the hard-block hooks.
- The three redacted retrospectives keep the earlier project's own register ids on purpose; the identifier gate excludes that folder and nothing else.
- The read-before-touch hook stays in `log` mode until its ledger is reviewed on 2026-09-28; flipping the mode is a reviewed commit, never a hand edit.
- The hook-settings gate requires every script under `tools/hooks/` to be registered; a new hook script turns `make check` red until a human registers it.
- A second live session in this checkout works in a git worktree until the one-session guard lands (incident of 2026-09-14).
- `main` receives only a tree `make check` stamped green: stage everything, run the check, commit, switch, merge fast-forward; untracked files block the stamp until they are added.
- The code-health ceilings move only through `make ratchet-bump` and `make ratchet-loosen`; a relaxation past its date is red until tightened or re-approved.
- The plan is corrected in place between versions and rewritten as a version once annotations accrete; retired sections get a home in the decisions log in the same change; the harness page's inventory is regenerated, never typed; a changed fact is swept through the `docs-sweep` skill, and a rewritten document gets a targeted rewrite of its section, never an appended note or a dated annotation. A build that deviates from a mechanism the plan names changes the plan in the same commit, with the reason.

## Waiting on Wes
Reddit credentials (keys in `.env.example`, pasted into `.env` by Wes) and the API access request before tranche B (the Responsible Builder Policy requires explicit approval; facts in `docs/reference/reddit-policy-facts-2026-09-14.md`); the QNAP remote; the pruning pass-one ruling (the list lives in the archive folder); the go for the one-live-session-per-checkout guard; the compliance bounds in `docs/decisions/DECISIONS.md` § 2 as a whole, now re-derived under the twice-weekly cadence; N-19 (no changelog ceremony) as a ruling; the data-controls opt-out on each consumer plan before the next external packet is uploaded; moving this session's transcript to the archive after it ends (guarded command in the archive folder's README); the suggestions Wes holds for the plan review, after this round's own list is presented. Details and recommendations: `docs/decisions/DECISIONS.md` § 2026-09-16, § 2026-09-15 and § 2026-09-14.
