---
purpose: The router; one line per document and when to read it, checked against the tree both ways.
update-policy: prune-stale
mirrors: [CLAUDE.md]
verified-at: M1a-A
---
# Thread Digest reference corpus — INDEX (the router)

> Purpose: one line per document and *when to read it*. Keep this file short. The corpus lives in the repo at `docs/` since M0 (2026-09-13) and travels with the code.
> Update policy: add a line when a document is added; remove it when the document is retired. The doc-currency test checks INDEX ↔ `docs/**/*.md` in both directions, so every file under `docs/` must appear below exactly once.

## Start here: which of these are you about to do?

| I am about to… | Read | Then |
|---|---|---|
| Start a session | `recent/STATUS.md` | `CLAUDE.md` § The irreversible few |
| Explain the system, or read about it for the first time | `OVERVIEW.md` | `THREADDIGEST_HARNESS.md` for the working method; `PLAN.md` for the design |
| Touch the schema, migrations, upserts, search index, backups | `learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` §1–§2 | `runbook/RUNBOOK.md` § Migrate; `.claude/rules/db.md` loads automatically |
| Change the collector (fetch, budget, revisit, reconcile, invariants) | `PLAN.md` § Collector algorithm | `decisions/DECISIONS.md` 2026-09-13 entries; `.claude/rules/services.md` |
| Touch deletion, scrubbing, or anything compliance-related | `PLAN.md` § Data model (state machine) | `runbook/KNOWN_ISSUES.md` |
| Change the web UI | `PLAN.md` § Web UI | `reference/reviews/2026-09-12-ui-design-review.md`; `.claude/rules/web.md` |
| Add or change a gate, ratchet, invariant, or test policy | `PLAN.md` § Robustness → Guard design rules | `runbook/GUARDS.md` (birth incident, positive control, verdict) |
| Write a brief for a sub-agent | `reference/AGENT_BRIEF.md` | the rows above for the task |
| Land a change on a migration, scrub, deletion, the upsert repository, a gate, a hook, or the working agreement | `reference/reviews/REGISTER.md` (a review row is required) | `PLAN.md` § Review harness |
| Decide whether to adopt a practice from the earlier project | `reference/earlier-project/EARLIER_PROJECT_REFERENCE.md` (the entry point) | `reference/reviews/2026-09-13-documentation-practices-assessment.md`; `reference/reviews/2026-09-13-harness-assessment.md` § 7 |
| Run a retrospective or score a prediction | `learnings/LEARNINGS_TRANSFER.md` §5 | `learnings/DOC_DRIFT_FINDINGS_2026-09-13.md` |
| Wonder why something was decided | `insights/INSIGHTS_2026-09-12.md` | `decisions/DECISIONS.md` (settled negatives first) |

## How the corpus is organized

| Folder | What lives here | Update policy | Read when |
|---|---|---|---|
| `docs/` (root) | `PLAN.md` (the plan; canonical for design), `OVERVIEW.md` (the system in ten minutes), `THREADDIGEST_HARNESS.md` (the working method, with a generated inventory), `TEST_STRATEGY.md` (router into the test specs), this file | Each document's front matter declares its policy (`versioned` for the plan and the strategy: corrected in place between versions, rewritten as a version once annotations accrete; `prune-stale` for the rest: the affected section is rewritten in place) and is checked by `tools/doc_policy.py` | Plan: before any design change. Overview: to explain the system. Harness: before adding a mechanism. Strategy: before writing or changing tests |
| `insights/` | Dated captures of strategy discussions and the reasoning behind decisions | Append-only, dated entries | Starting a new phase, or when a decision is being questioned |
| `learnings/` | Hard-earned lessons: dated "applied" notes now; a canonical curated learnings file is added when the first project-native lesson lands | Append-only; canonical file curated | Before touching the area the lesson is about (routing table says which) |
| `decisions/` | `DECISIONS.md`: settled choices, settled negatives, compliance bounds, Postgres-exit triggers, threat model, each with a "revisit when" | Append-only; dated entries | Before proposing an alternative to something already decided |
| `reference/` | External and immutable material: the research report, the earlier project's retrospectives, reviewer and panel reports | Add, never edit | When the source of a claim is needed |
| `runbook/` | `RUNBOOK.md`, `KNOWN_ISSUES.md`, `GUARDS.md`; a data dictionary generated from `schema.sql` joins at M1a | Prune-stale; generated where possible | Operating, debugging, adding a guard, fixing a bug |
| `recent/` | `STATUS.md`: what is in flight and what is next | Rewritten, never appended | Start of every working session |

## Routing table (task → read first)

| If you are about to… | Read |
|---|---|
| Start a session | `recent/STATUS.md`, then the row below that matches the task |
| Write or change any test, or decide what to test first | `TEST_STRATEGY.md` (spec table and status), then the panel report it points at |
| Touch the database schema, migrations, upserts, FTS, backups | `learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` §1–§2, `reference/reviews/2026-09-13-panel-db-integrity.md` (§A specs, §C migration checklist, §D fingerprint), `runbook/RUNBOOK.md` § 4 |
| Touch deletion, scrubbing, reconcile, or anything compliance-related | Plan § Data model (content-state machine), `decisions/DECISIONS.md` § 2 (compliance bounds), `reference/reviews/2026-09-13-panel-ingest.md` §B.6–B.7, `runbook/KNOWN_ISSUES.md` |
| Add, change, loosen, or retire a gate, ratchet, invariant, or hook | Plan § Robustness → "Guard design rules" and "Gates and ratchets", `THREADDIGEST_HARNESS.md`, `reference/reviews/2026-09-13-panel-enforcement.md` §B–§D, `runbook/GUARDS.md`, `runbook/RUNBOOK.md` § 6 |
| Change the collector's fetch, budget, revisit, reconcile, or search behavior | Plan § Collector algorithm, `reference/reviews/2026-09-13-panel-ingest.md` §A (fake API) and §B, `reference/reviews/2026-09-12-collector-design-review.md` §1 |
| Change the web UI, middleware, or an operator flow | Plan § Web UI, `reference/reviews/2026-09-13-panel-ui.md` (§A specs, §B operator checklist, §C setup threat model), `reference/reviews/2026-09-12-ui-design-review.md` |
| Fix a bug | `runbook/KNOWN_ISSUES.md` (add the row; failing test first) |
| Deploy, migrate, restore, or run the quarterly review | `runbook/RUNBOOK.md` |
| Wonder why something was decided the way it was | `insights/INSIGHTS_2026-09-12.md`, then `decisions/DECISIONS.md` |
| Evaluate a new approach, library, or "just add a check" | `decisions/DECISIONS.md` § 3 settled negatives first; `reference/reviews/2026-09-13-adversarial-review.md` §C (over-engineering cut list) |
| Question a Reddit-side behavior | `reference/2026-09-11-compass-research-report.md`; `reference/reviews/2026-09-13-panel-ingest.md` §C (probe plan) |

## Documents in this corpus

### Root

- `PLAN.md` — the plan, version 2 (2026-09-15): intent and the question the system answers, how the data becomes insight, architecture and module map, data model and state machine, collector algorithm and cadence, workspaces, CLI and web UI, testing strategy, robustness and enforcement, release and deployment, milestones, things only Wes can do. Read when: any design question; it is canonical. History lives in `decisions/DECISIONS.md` and the review records, not here.
- `OVERVIEW.md` — the system in ten minutes for a reader who will not read the plan: what it is, the one question, how the data becomes insight (our own ranking, authored themes, the discovery signals, denominators), workspaces, what it is not. Read when: explaining the system, or before the plan.
- `THREADDIGEST_HARNESS.md` — the working method, plain English first: what the harness is for, its strengths, how it came to be, the four parts (memory and context, documentation, enforcement, agent discipline) each with a plain-English lead and the detail after, how they hold each other, what is deliberately absent, and a generated inventory of every mechanism in the tree (gated, G54). Read when: adding or changing a mechanism, or explaining how the project keeps an agent-built codebase honest.
- `TEST_STRATEGY.md` — v1 router into the five panel reports: layered policy, kinds of tests and sweeps, every spec ID with layer/phase/priority/status (cut list applied), the M0 gate set and M1a invariants, guard design rules, open owner questions. Read when: writing tests or deciding test order.
- `INDEX.md` — this router.

### `insights/`

- `insights/INSIGHTS_2026-09-12.md` — the kickoff strategy discussion: Wes's priorities, insights, every decision and its reasoning, the 2026-09-13 additions, principles worth keeping. Read when: a decision is questioned or a new phase starts.

### `learnings/`

- `learnings/DB_LEARNINGS_APPLIED_2026-09-12.md` — ranked adoption of the earlier project's database lessons with confidence/importance/value/cost, the guard design rules, what was declined, decisions raised, lessons that did not transfer, the living follow-up list. Read when: touching `db/`, migrations, test isolation, or invariants.
- `learnings/LEARNINGS_TRANSFER.md` — what Thread Digest took from the earlier project beyond the database (guard philosophy, testing and operational methodology, documentation and memory structure, agent practice), the interpretive layer on the database adoptions, what was declined and why, and dated predictions to score at the retrospective. Read when: starting a retrospective, judging whether a transferred lesson held, or starting the next project's transfer.
- `learnings/DOC_DRIFT_FINDINGS_2026-09-13.md` — the documentation drift found in our own corpus on 2026-09-13 (retired gates still listed as live, self-contradicting counts, schedules of cut items, narrated status, dangling register ids), each with a way to scan for it in another system. Read when: sweeping documents after a decision, or auditing another project's corpus.

### `decisions/`

- `decisions/DECISIONS.md` — settled choices (D-nn), compliance bounds, settled negatives (N-nn), Postgres-exit triggers, LAN threat model, reviewer-question decisions, and the pending-Wes list. Read when: before proposing an alternative or claiming compliance.

### `runbook/`

- `runbook/RUNBOOK.md` — setup, daily operation from the UI, deploy, migrate, restore drill, quarterly guard review; steps known so far, later-milestone steps marked. Read when: operating or deploying.
- `runbook/KNOWN_ISSUES.md` — fixed bugs with root cause and the regression test node id (currency-checked). Read when: fixing a bug or seeing a familiar symptom.
- `runbook/GUARDS.md` — the guard ledger: Active (M0 shipped set with birth incident, mechanism, positive-control node, verdict), external controls with "last seen red", Retired, Loosenings. Read when: adding, loosening, or reviewing a guard.

### `recent/`

- `recent/STATUS.md` — what is in flight on the stated date, what is deferred, what waits on Wes, next steps. Read when: every session start.

### `reference/`

- `reference/2026-09-11-compass-research-report.md` — the Compass research report on building a rules-compliant Reddit reader: OAuth/PRAW constraints, rate limits, deletion handling, IP considerations, the minimal-start recommendation. Read when: a Reddit-side rule or limit is in question.
- `reference/AGENT_BRIEF.md` — the template every sub-agent brief follows (purpose, tier, read-first rows, rules that bite, scope, contract, verification, don'ts). Read when: writing or reviewing a brief.
- `reference/reddit-policy-facts-2026-09-14.md` — Reddit's Data API wiki, Data API Terms, Developer Terms, and Responsible Builder Policy as read from the live pages on 2026-09-14: the deletion obligations, the forty-eight-hour recommendation, the approval-before-access rule, the commercial-use boundary, the rate limit, with revision dates. Read when: designing reconcile, scrub, backups, or exports; before the tranche B access request; and at each quarterly review (re-read the pages).

`reference/earlier-project-retrospectives/` — three of the earlier database project's eight retrospective documents, kept redacted as narrative source material (the other five live only in the archive at `~/repos/insightminer-desktop-archive/Reddit/Database_Key_Learnings/`, readable by agents on this machine; disposition decided 2026-09-13, see `REDACTION_NOTE.md` there and `learnings/LEARNINGS_TRANSFER.md` §7):

- `reference/earlier-project-retrospectives/INDEX.md` — what each retrospective covers and the shared meta-lesson. Read when: choosing which retrospective to open.
- `reference/earlier-project-retrospectives/REDACTION_NOTE.md` — what was redacted in the three kept files, which five files moved to the archive and why, and the history rewrite. Read when: sharing the repo, or looking for one of the moved files.
- `reference/earlier-project-retrospectives/CRITICAL_FAILURES_RETROSPECTIVE.md` — the ranked ~15 failures; a number that was real but measured wrong, scoped wrong, or asked the wrong question. Read when: designing a metric or invariant.
- `reference/earlier-project-retrospectives/WHY_THE_GUARDS_EXIST.md` — what incident birthed each guard, what it caught since, and the design rule separating guards that work from ones that only look like they work. Read when: adding or reviewing a guard.

`reference/reviews/` — independent reviewer and panel reports (raw, unedited):

- `reference/reviews/2026-09-12-collector-design-review.md` — collector, data model, PRAW behavior verified against upstream, failure-mode matrix, migration practices. Read when: changing fetch, paging, or the adapter.
- `reference/reviews/2026-09-12-ui-design-review.md` — the Reddit-style local web UI design review: routes, wireframes, nested comments, settings UX, Run now, export, search, testing, styling. Read when: changing the UI.
- `reference/reviews/2026-09-12-db-learnings-review-A.md` — reviewer A's extraction from `KEY_LEARNINGS`, `DEAD_ENDS_AND_RULED_OUT`, `VALUE_STAGE_KEY_LEARNINGS` (failure catalogue cited as "A #nn"). Read when: an "A #nn" citation needs its source.
- `reference/reviews/2026-09-12-db-learnings-review-B.md` — reviewer B's extraction from `SYSTEM_ARCHITECTURE_AND_REBUILD`, `PROJECT_JOURNEY` (cited as "B Snn/Fnn/Inn/Tnn"). Read when: a "B …" citation needs its source.
- `reference/reviews/2026-09-13-panel-db-integrity.md` — database panel: 55 specs (DB-01…55), three empirical SQLite corrections, fixture-DB plan, migration checklist, `schema.sql` and fingerprint design, judgements, owner questions. Read when: touching `db/`.
- `reference/reviews/2026-09-13-panel-ingest.md` — ingest panel: `FakeRedditGateway` scenario-builder API, 60 specs (SW/SS/TE/TR/RV/RC/SC/FR/PA/NM/RL/JS/TH/DG/CF/AD/GT-nn), probe plan (P-01…17), judgements (D-1…18), owner questions. Read when: touching the collector or the fake.
- `reference/reviews/2026-09-13-panel-enforcement.md` — enforcement panel: CI design, 38 gates (G01…38) with positive controls, ratchet file format and one-way protocol, hooks, cut list, compensating controls, `GUARDS.md` structure, owner questions. Read when: touching CI, ratchets, hooks, or `tests/gates/`.
- `reference/reviews/2026-09-13-panel-ui.md` — UI panel: 55 specs (UI-01…55), operator-complete checklist, setup-wizard threat model, plan judgements, owner questions. Read when: touching `web/`.
- `reference/earlier-project/EARLIER_PROJECT_REFERENCE.md` — the self-contained reference on the earlier project: how it and its harness worked, what failed and why, what was measured, what this project adopted, skipped, and deferred, the predictions to score; the entry point, with the assessments below as its appendices (abstracted; the identifier gate scans it)
- `reference/reviews/2026-09-13-adversarial-review.md` — the adversarial pass: unfalsifiable gates, gaps against the failure catalogue, over-engineering cut list, contradictions, unverified claims, disagreements with the ranked learnings, risks, top 10 changes. Read when: tempted to add a gate, or when a plan claim needs a second opinion.
- `reference/reviews/REGISTER.md` — the review register: one row per review run (trigger, scope as commits, provider and tier, verdict, record); machine-read, so a commit on a review-required surface since 2026-09-15 without a row is red
- `reference/reviews/templates/external-deep-research.md` — the refute-framed brief an outside reviewer receives with a packet; rendered by `tools/review_packet.py` with the commit and packet hash; a gate artifact, reviewed like code
- `reference/reviews/templates/claims.md` — the load-bearing claims with the test that backs each, machine-read by the citation gate; travels in every review packet so a reviewer can try to falsify them
- `reference/reviews/2026-09-14-packet-brief-panel.md` — the internal panel on the review packet and its brief before the first external send: what each seat was given, seventeen packet findings and their outcomes, two verdicts held for Wes, eight system findings from the dry run (two reproduced)
- `reference/reviews/2026-09-14-testing-and-workflow-panel.md` — the five-seat panel on the testing methodology, operator workflows, unattended failure modes, the change workflow, and SQLite: what changed the same day, what is queued by area, and five rulings requested
- `reference/reviews/2026-09-14-external-round-1.md` — the first external round: three deep-research reports and one partial code-executing run on the packet at 616f9b9, every finding re-run against the tree; six defects confirmed (KI-015 to KI-020), secure-delete proposed, seven claims refuted, four rulings held for Wes. Read when: triaging the next external round, or before touching restore, the crosspost path, the sweep's stop reasons, or the hooks.
- `reference/reviews/2026-09-15-doc-policy-review.md` — the refute pass on the document-contract mechanism the day it was built: sixteen bypasses and vacuous passes, the controls that proved the wrong thing, the overstated documents, and what changed the same day (the merge-base baseline, records checked, the accretion count scoped to prose, mirrors declared both ways). Read when: changing `tools/doc_policy.py`, or adding a check that reads a document's own declaration.
- `reference/reviews/2026-09-15-plan-v2-review.md` — the refute pass on plan version two against version one: what the rewrite had lost, what it stated falsely against the tree, the two cadence residues found in code (KI-025, KI-026), the gate widenings, and what changed. Read when: rewriting a document as a version, or judging what a review seat that reads the code finds that a document-reading seat does not.
- `reference/reviews/2026-09-15-round-one-fix-panel.md` — the internal panel on the round-one fixes: three Opus seats (test-methodology, adversarial correctness, hooks/migration) reviewed `ffa63f1..HEAD`; four hook holes and four correctness follow-ups closed, no fix reverted. Read when: touching the hooks, the crosspost or deletion paths, the doctor source check, or the fake's tree expansion.
- `reference/reviews/2026-09-16-identifier-resolution-review.md` — the refute pass on the identifier resolution added to the document contract (G55) and the backtick widening of G34: the column check found dead against the committed schema, the exemptions found line-wide, the annotation route found uncounted, eleven findings and the outcome that closed them before landing. Read when: changing `tools/doc_policy.py` or judging what the identifier check can and cannot see.
- `reference/reviews/2026-09-16-one-session-guard.md` — the build and refute pass on the fifth hook, the one-live-session-per-checkout lock (G57): what it does on each event, how the ten controls plant the bad state, why the registration entry joins an existing matcher rather than adding a second, and the six claims refuted with how.
- `reference/reviews/2026-09-16-plan-v2-deep-review.md` — the summary record of the plan-version-two deep review Wes asked for: the claims, the seven seats, the findings weighed by evidence, what was corrected the same day, and what waits on Wes. Read when: judging whether the plan describes the system that exists, or before the milestone pass at M1b.
- `reference/reviews/2026-09-16-plan-v2-deep-review-A1-disposition.md` — seat A1 (mechanical): every unit of plan version one with its disposition in version two: carried, moved, reduced, absent, superseded, and the units new in version two. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-plan-v2-deep-review-A2-loss-judgement.md` — seat A2: the reduced and absent units judged for lost value; the invariant severity split, the zero-suppression rule, and two decisions-log rows stating the retired remote plan. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-plan-v2-deep-review-B1-rounds-honored.md` — seat B1: the 2026-09-12 and 2026-09-13 review records' accepted findings traced into version two; the content-state contradiction and the dropped deleted-link-post rule. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-plan-v2-deep-review-B2-rounds-honored.md` — seat B2: the assessments, panels, external round one, and the version-two reviews traced; the invariant severity claim, the packet's missing documents, the probe-day checklist with no home. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-plan-v2-deep-review-C-changes.md` — seat C: each version-two change asked whether it was reasoned to version one's standard; the reconcile budget fit, the deletion window under the cadence, D-30's missing trigger. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-plan-v2-deep-review-D-readiness.md` — seat D: readiness for tranche B and M1b; fixture scrubbing undecided, the probe command under-specified, no probe-day checklist, the live suite unable to pass, the cassette directory undecided. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-plan-v2-deep-review-E1-collector-claims.md` — seat E1: every behavioural claim about the built collector checked against code and tests; the severity claim, the nonexistent degraded status, the unwired removal signal and per-run backup. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-plan-v2-deep-review-E2-storage-claims.md` — seat E2: every behavioural claim about storage, migrations, backups, health and deployment checked; the unbuilt retention story, the no-source run, the freshness clause. Read when: the summary record's finding cites it.
- `reference/reviews/2026-09-16-private-memory.md` — the record of the working notes leaving the public repository: the ruling, the five mechanisms that read the committed snapshot, what each one does now, what the move costs (a clone carries no memory; the routing check over memory narrows to a machine that has it), and what an independent seat should attack. Read when: wondering why a document cites a commit hash git does not have, or why the memory is not in the tree.
- `reference/reviews/2026-09-16-project-rename.md` — the record of the project rename: what moved, what deliberately did not (the repository folder, the reference records, the existing lines of the append-only logs), the four prose lines rewritten by hand, and how the renamed tree was proven green. Read when: tracing why a name in the git history differs from the name in the tree, or why the review register sees less history than it once did.
- `reference/reviews/2026-09-16-probe-command.md` — the record of the probe command's offline half: the five capture modes, `save()` behind `--save-fixture`, the three planted positive controls that back it (the unconditional scrub, the data-directory write check, the command tree), the group-level `--gateway`/`--fixture` and why they sit there, and what only the probe day can verify. Read when: preparing the probe day, changing `services/probe.py`, or judging what an offline test of a capture command can prove.
- `reference/reviews/2026-09-16-push-rule.md` — the record of Wes's ruling on the push rule: what the blanket refusal was for, why the pre-push check makes a plain push to `main` the gate rather than a bypass of it, where the new boundary sits (force, delete, `--all`/`--mirror` and `--no-verify` still refused), what proves it, and the three things an independent seat should attack. Read when: changing `tools/hooks/no_bypass_git.sh`, or wondering why the working agreement no longer says never push to `main`.
- `reference/reviews/2026-09-16-doc-policy-record-compare.md` — the record of KI-028: the reference-record check compared a decoded baseline with an encoded working file, so the hash map the history rewrite wrote read as edited on every run; why a branch-only run of a merge-base check says nothing about `main`, and what the fix does not narrow. Read when: changing `tools/doc_policy.py`, adding a record that is not Markdown, or judging where else a baseline is compared against a working file.
- `reference/reviews/2026-09-16-ci-first-run.md` — the record of the first CI run on the remote: three defects a second kind of machine found while `make check` was green on the Mac (an undeclared zone database, a hash-map gate asking existence of commits only the rewriting machine has, a duplicate-code hit named by the wrong file), the route chosen for the map and why the other was refused, and what an independent seat should attack. Read when: a gate is green on one machine and red on another, changing `.github/workflows/ci.yml`, or judging whether a CI difference is a defect or a tolerance.
- `reference/reviews/2026-09-16-known-issues-rows-in-comment.md` — the record of KI-034: sixteen known-issues rows sat where the gate could not see them (thirteen inside the format-example comment, three below a blank line, one of those written onto another row's line), why the comment strip and the line-keyed parser hid them, how the gate now requires every row-shaped line to be a parsed row with a floor under the shape and no second row on a line, what the independent seat and the landing seat each refuted, and the holes left open. Read when: adding a row to a register the gates parse, changing `tests/gates/test_known_issues_cite_collected_tests.py`, or judging whether another register can lose a row the same way.
- `reference/reviews/2026-09-16-ci-second-run.md` — the record of KI-035: the second CI run's one failure, where the review-register gate's baseline went to git's approximate date parser and so meant the baseline date at the hour the suite ran, in the host's zone; the brief's unreachable-objects hypothesis refuted by a fresh clone, the six commits the sliding cutoff was hiding, the moving reference that had shrunk their review row, the range pair that was not an ancestry, and what an independent seat should attack. Read when: a gate reads a date or a commit list, a scope or citation is written against a history that has been rewritten, or a gate is green on one machine and red on another.
- `reference/reviews/2026-09-16-digest-permalinks.md` — the record of KI-036: every post link the digest rendered resolved against the local server, because `posts.permalink` stores the site-relative path Reddit returns; which reads hand a permalink to the digest (one), why the stored column was left as the collector wrote it, why no helper wraps the single call site, and why the digest golden did not move. Read when: changing `ranked_posts` or another read the digest renders, or deciding where a value should be joined on — at the read or at each rendering.
- `reference/reviews/2026-09-17-run-row-settings-warnings.md` — the record of revision 0005: the run row records the resolved non-secret settings beside their fingerprint and the warnings it raised beside the invariants' verdicts; how the 0004 fixture database was generated, why one serialization feeds both the column and the hash, why `[]` and NULL say different things in two more places, and why a column a new revision adds may not be named on the run-row insert or on the heartbeat (KI-039). Read when: writing a migration that touches `runs`, or deciding where a run's own facts should be written during a run rather than at its close.
- `reference/reviews/2026-09-17-private-term-list.md` — the record of the G35 widening that checks tracked text against the operator's private term list: why the list cannot live in a public repository and what that costs, why the guard was widened rather than added, why an acceptance is the hash of a line, and why a missing list is a printed note while an unusable one is red. Read when: changing the identifier gate or its file set, or deciding how a check should behave where the thing it reads is absent rather than wrong.
- `reference/reviews/2026-09-17-tier-table-refactors.md` — the record of the tier-table change that names structural refactors (complexity reduction, module splits, boundary moves) as Opus work under two conditions, characterization tests first and a fresh-context readability review of the diff, while renames, moves and codemods stay Sonnet work. Read when: writing a refactor brief, or deciding the tier of a stage that looks mechanical and is not.
- `reference/reviews/2026-09-17-code-panel-since-baseline.md` — the three-seat panel over the code built since 2026-09-15: the ranked findings with their fix rounds (two P0: the scheduled entry point that cannot start, a rate limit in the date form classified fatal), what held under re-running, what the demo corpus hides, and the cut list; the seat memos sit beside it as `2026-09-17-code-panel-seat-A.md`, `-seat-B.md`, `-seat-C.md`. Read when: fixing one of its findings, planning the next panel, or asking what the demo cannot show.
- `reference/reviews/2026-09-17-code-panel-seat-A.md` — the correctness-and-tests seat of the code panel above: whether each shipped test asserts its strategy row's claim, the hard cap that bounds the limit but not the spend, the counting mechanism two docstrings describe that does not exist, and the weakest test in the tree. Read when: fixing findings A1–A10, or judging what a budget or run-lifecycle test actually proves.
- `reference/reviews/2026-09-17-code-panel-seat-B.md` — the operator-and-data-safety seat of the code panel above: the scheduled entry point that cannot start, the credential-adjacent values on the run row, the two writes outside the data directory, the log path, the security header, and ten checks that held (escaping, the Host check, the backup before a migrate). Read when: fixing findings B1–B7, or changing deployment, settings typing, or the web perimeter.
- `reference/reviews/2026-09-17-code-panel-seat-C.md` — the adversarial seat of the code panel above: the rate limit in the date form classified fatal, the routing gate that flips on a clean tree, three planted violations that passed every gate, the tree builder's double count, the digest's bare numbers, and what the demo corpus hides. Read when: fixing findings C-1–C-9, or asking what a guard would not catch.
- `reference/reviews/2026-09-17-m1b-comment-writes.md` — the record of M1b's database layer: the comment upsert and its parent pass, the wholesale stub replacement, the tree stamp, the three new ownership rows, and the due-queue order; why a terminal deletion state freezes a row's content in the statement rather than in the values a caller builds, why an update-only ownership row declares that it never inserts, and which three points of the brief were deviated from. Read when: writing to `comments` or `comment_more`, adding an ownership row for a new ingest path, or deciding where a rule the scrub service depends on should be enforced.
- `reference/reviews/2026-09-13-harness-assessment.md` — the evidence-first assessment of the earlier project's personal harness (memory and context model, documentation system, enforcement, agent discipline): a mechanism catalogue with birth incident, evidence, cost, and a transfer call each; what to keep and what to simplify with high confidence; what only Wes can settle; revised after a steelman pass that defended the original harness. Read when: deciding whether to adopt a practice from the earlier project, or pruning the reference material.
- `reference/reviews/2026-09-13-documentation-practices-assessment.md` — which of the earlier project's documentation and memory practices to adopt, adapt, or leave, from a usage census of 1,127 transcripts and a reading of its documentation system: the day-one set for any new project, the skip list with the measured costs, and eight decisions. Read when: adding a document class, a register, or a memory mechanism.
- `reference/reviews/2026-09-13-harness-questions-answered.md` — the 26 harness questions answered from the earlier project's repository backup, transcripts, and never-committed material (19 at high confidence): what fired versus what redirected work, installed versus written, read versus written, used versus defined; the catalogue rows the evidence moves; grounded recommendations for the earlier repository; ten decisions for Wes. Read when: re-scoring a harness catalogue row, or deciding whether an advisory mechanism is worth building.
