# Review register (machine-read by `tests/gates/test_review_register.py`)

> One row per review run, appended, never edited except to correct a typo. A commit on `main` dated on or after 2026-09-15 that touches a review-required surface (`src/insightminer/db/migrations/`, `src/insightminer/services/scrub.py`, `src/insightminer/core/deletion.py`, `src/insightminer/db/repo.py`, `tests/gates/`, `tools/hooks/`, `CLAUDE.md`, `.claude/settings.json`) (the four package paths are as they were written; the package was renamed on 2026-09-16 and each is `src/threaddigest/...` in the tree today. The surfaces the gate actually reads are its own list in `tests/gates/test_review_register.py`, never this sentence; this register is append-only by the diff, so the line is corrected by this parenthetical rather than rewritten) must be covered by a row's **Scope** (a commit hash, or a `first..last` range), and every **Record** must be a file in this folder. The gate names the uncovered commits; it cannot judge whether the review was adversarial enough, so the record says what the reviewer was given (the packet and its hash) and who gave the verdict.
>
> Triggers, in Wes's words (2026-09-14): a complex problem solved, a significant design plan finalised, a complex or risky fix confirmed, a module finished or started; and, from the harness protocol, any change to the enforcement surfaces, a migration, the intent block, or the irreversible four. Reviews are refute-framed; findings are weighed by independence and evidence, never tallied; a reviewer handed the prior verdict inherits its blind spots. An external provider (a reviewer outside this model family) is used at the moments `docs/decisions/DECISIONS.md` § 2026-09-14 lists; nothing but the clean current tree or a diff ever leaves this machine.
>
> Rows dated before the baseline are the planning-day panels, listed for provenance; their scope is `pre-baseline`.

| Date | Trigger | Scope | Provider / tier | Verdict | Record |
|---|---|---|---|---|---|
| 2026-09-13 | plan lock-down: five test-strategy panels and one adversarial pass | pre-baseline | in-family, Opus, fresh context | adopted per `docs/PLAN.md` § Adversarial review | `2026-09-13-adversarial-review.md` |
| 2026-09-13 | the earlier project's harness and documentation practices | pre-baseline | in-family, Opus, fresh context | adopted per `docs/decisions/DECISIONS.md` | `2026-09-13-documentation-practices-assessment.md` |
| 2026-09-14 | the external review packet and its brief before the first send (a prompt template is a gate artifact) | fe0f9d8 | in-family, three seats (top tier dry run; Opus critic; Opus separation auditor), fresh context, packet-only | seventeen packet findings applied, two auditor verdicts held for Wes, eight system findings recorded (two reproduced) | `2026-09-14-packet-brief-panel.md` |
| 2026-09-14 | testing methodology, operator workflows, failure modes of unattended operation, the change workflow, and every SQLite-facing decision, before comment trees and the first external send | ee955bb | in-family, five seats (Opus auditor, top-tier workflow seat, Opus adversary, Opus principal engineer, Opus SQLite specialist with reproductions), fresh context | eight findings changed the tree the same day (duplicate ids, the merge hook's hole, the surviving stamp, and three new open issues); the rest ordered on the status page's queue; five rulings requested | `2026-09-14-testing-and-workflow-panel.md` |
| 2026-09-14 | external review round one: three deep-research reports and one partial code-executing run on the packet at 616f9b9 (packet hash in the record) | 616f9b9 | external, four seats labelled A to D, packet-only, refute-framed brief | six defects confirmed by reproduction (KI-015 to KI-020), FTS5 secure-delete proposed, seven claims refuted and swept, four rulings held for Wes | `2026-09-14-external-round-1.md` |
| 2026-09-14 | external review round one, the fourth seat's full report (thirteen findings with a reproduction script; the script read, every new claim reproduced with the session's own code) | 616f9b9 | external, code-executing seat D | four more defects confirmed (KI-021 to KI-023 and the C-05 overstatement), one model-contract gap queued for M1d, two claims reworded, two hook shapes folded into KI-020 | `2026-09-14-external-round-1.md` |
| 2026-09-15 | internal panel on the round-one fixes, cadence, and KI-011 before moving on (a complex fix set on the hooks and collector) | a0d8e8a..HEAD | in-family, three seats (Opus test-methodology, Opus adversarial correctness, Opus hooks/migration adversarial), fresh context | four hook holes closed (line continuation, --config-env, double-slash, case), four correctness follow-ups (KI-016/017/021/023), three enforcement-reach gaps closed; no landed fix reverted | `2026-09-15-round-one-fix-panel.md` |
| 2026-09-15 | plan version two (a significant design document rewritten), the harness page and gate G54, the widened gates G34 and G44, and the working-agreement rows that came with them | a572c5f | in-family, two seats: a Sonnet inventory of the old plan against the tree, then a fresh-context Opus refute pass of version two against version one and the tree, read-only | the completeness claim was rejected and every finding re-checked and applied: two cadence residues in code and configuration (KI-025, KI-026), eleven false statements corrected, five lost items restored, ten mirrors swept | `2026-09-15-plan-v2-review.md` |
| 2026-09-15 | the document and memory process mechanized (a new gate G55 on the writing side of the corpus, the docs ratchet family, memory rules, the working-agreement rows and routing) | a9e9ec2 | in-family, one fresh-context Opus seat, read-only, refute-framed, with sandbox reproductions of every bypass | all five claims rejected on the first build: sixteen bypasses or vacuous passes, eight controls proving the wrong thing, twelve overstatements, nine conflicts; every finding re-checked and closed before landing (merge-base baseline, records checked, prose-only accretion, mirrors declared both ways, controls on a real git tree, honest wording) | `2026-09-15-doc-policy-review.md` |
| 2026-09-16 | identifier resolution added to the document contract (G55 widened to resolve every code identifier a rewritten document names, two new docs ceilings), G34 reading through backticks, the design-to-build sweep, and the working-agreement rows | 7e8412c..73e071c | in-family, one fresh-context Opus seat, read-only, refute-framed, with worktree reproductions of every bypass | one critical (the column check never fired against the one-line committed schema; its control tested a fixture), two high (exemptions line-wide, the repaired lines inside them; the annotation route uncounted), five medium, three low; every finding closed before landing in 73e071c (parenthesis-matched schema parse with a control on the real file, per-token neighbourhoods, a third ceiling, bare names against the file list and string literals, honest counts and wording) | `2026-09-16-identifier-resolution-review.md` |
| 2026-09-16 | plan version two, the deep review Wes asked for before the work starts (was anything of version one lost; was version two and each of its changes scrutinised to version one's standard; are the hooks, the harness and the testing methodology aligned for tranche B), plus the corrections it produced on the plan, the ledgers, the packet builder and two gates | 26ca5db | in-family, seven fresh-context seats on a shared claim set: one Sonnet inventory (the version-one disposition table) and six Opus refute seats (loss judgement, two rounds-honoured traces, the changes scrutinised, readiness, two claims-against-code passes), read-only | the rewrite lost no reasoning and the harness is ready; the plan stated the opposite of the code on invariant severity, the content-state rule order, the no-source run and backup retention, and tranche B's specification had five holes; every obviously wrong statement corrected in place the same day, the probe day specified as runbook § 9, the packet's missing documents carried with a two-directional gate (KI-027), eight decisions recorded with recommendations for Wes | `2026-09-16-plan-v2-deep-review.md` and the eight seat records beside it |
| 2026-09-16 | the rulings Wes delegated after the deep review (index membership a failure, fixtures scrubbed on save with the identifier gate widened, the live suite's escape with the pytest-config gate widened, the wrapper quiet on partial, the first-miss re-check, the budget, the digest's duration line, the ladder) | 89dcf85 | in-family, the main session, each ruling checked against the code it touches; the widened gates carry planted controls, and the fixture scrub has its own unit tests | every ruling stated in the plan and the decisions log with its reason; the severity change and the wrapper change asserted by their existing tests, updated to the ruling with the reason in the diff | `2026-09-16-plan-v2-deep-review.md` (the round that produced the questions) |
| 2026-09-16 | the fourth hook, a log-first Stop hook that refuses to end a turn when a document gained a question for Wes the message did not present (G56), its tests, the widened registration gate, and the working-agreement row | 3db5ec4 | in-family, the main session; five controls drive the script the way the agent runtime does, on a git repository with a transcript | log-first with a mode file; registration is the human's edit and the settings gate stays red until it lands | `2026-09-16-plan-v2-deep-review.md` (the round that set the rule) |
| 2026-09-16 | registration of the stop-time question hook in the hook settings (a human edit; the fourth hook, G56) | 901ffe1 | the human ran the generated script; the settings gate proves the entry and hooks_status counts it | the settings test green, four of four scripts registered | `2026-09-16-plan-v2-deep-review.md` (the round that set the rule) |
| 2026-09-16 | the project rename (the package, the CLI command, the environment prefix, the launchd labels, the database file name, the harness page and the prose), which touches `tests/gates/`, `CLAUDE.md` and, by path, `db/repo.py` and `db/migrations/` | 662b59b | in-family, the main session; a parametrized script whose dry run was read in full, exercised on a throwaway clone until `make check` was green before it was run here | no behaviour change and none found; the repository folder, the reference records and the existing lines of every append-only document untouched; four prose lines rewritten by hand and one redundant digest heading shortened; the register gate's reach over pre-rename history narrows, recorded as a follow-up | `2026-09-16-project-rename.md` |
| 2026-09-16 | the PRAW adapter's offline half (a module started and finished), the import-linter contract's flip from warn to error, and the planted control for the `praw` half of G05 that the ledger recorded as uncontrolled | 9db0f03 | in-family, the session that built it under a bounded brief, read against the installed library source rather than from memory; not an independent seat, which is the weakness of this row | the translation table built as specified, with three additions the library forced: a bare `KeyError` for a 401 prawcore's three-entry OAuth table cannot name, a `ValueError` out of `Retry-After`'s HTTP-date form, and a counting session required alongside an injected client so the budget can never read zero; the mypy override deleted once strict mypy came back green against the libraries' real types | `2026-09-16-plan-v2-deep-review.md` (the round that specified tranche B and the probe day) |
| 2026-09-16 | the probe command built (a module started and finished) with `--save-fixture` and `--blank-bodies`, which touches `tests/gates/` | 3336e81 | in-family, the session that built it under a bounded brief, offline against the fake gateway; not an independent seat, which is this row's weakness | three planted positive controls, each reverted and green again: `save()`'s own unconditional scrub, the data-directory write check on `--save-fixture`, and the command tree; every wire shape stays unverified until the probe day, which is what the modes exist to capture | `2026-09-16-probe-command.md` |
| 2026-09-16 | the working notes taken out of the public repository (D-35): the snapshot tool repointed at a private home, `tests/gates/` and `CLAUDE.md` touched, and the history rewritten to purge the folder | 473db66 and the rewrite commit that follows it | in-family, the session that made the change, rehearsed end to end on a throwaway clone; not an independent seat, which is this row's weakness | the two gates that resolve commit hashes were widened rather than narrowed, with controls on both halves of the map; G44's reach narrows to a machine that has the memory, recorded on its ledger row | `2026-09-16-private-memory.md` |
| 2026-09-16 | KI-028, the reference-record comparison fixed the day the history rewrite landed (a confirmed defect on `main`, and it touches `tests/gates/`) | b686eb9 | in-family, the session that ran the rewrite; not an independent seat, which is this row's weakness | the failing test came first and was watched go red on the unfixed tool; its second half is the control in the other direction, that a real edit to the same record is still reported, so the fix cannot have bought silence by making the check see nothing; the existing control on an edited Markdown record was re-run unchanged | `2026-09-16-doc-policy-record-compare.md` |
| 2026-09-16 | Wes's ruling on the push rule, which changes the hard-block hook, its gate file and the working agreement's irreversible-four neighbourhood (an enforcement-surface change) | 012b602 | in-family, the session that made the change under a bounded brief; not an independent seat, which is this row's weakness | the boundary drawn where the pre-push check can hold it: the plain push to `main` allowed, `--no-verify`, `--all`/`--mirror` and every rewriting or deleting form still refused, each refusal naming the part of the rule it fails; both halves of the new control watched go red against a hook with that branch disabled | `2026-09-16-push-rule.md` |
| 2026-09-16 | the first CI run on the remote went red on Linux while `make check` was green on the Mac; three confirmed defects fixed, each touching `tests/gates/` | e463bb7..c54f683 | in-family, the session that made the change under a bounded brief, working from the failed run's logs; not an independent seat, which is this row's weakness | every cause a real defect rather than a CI difference to tolerate: an undeclared zone database (KI-031), a hash-map gate asking existence of commits only the rewriting machine has (KI-032), and a duplicate-code hit named by whichever module pylint's run finished on (KI-033); the map was narrowed rather than edited, because it is a reference record | `2026-09-16-ci-first-run.md` |
| 2026-09-16 | KI-034, sixteen known-issues rows the gate could not see (a confirmed hole in a gate, and it touches `tests/gates/`) | 9182a2c..72090f2 | in-family; an independent seat in a fresh context, given the first commit and a refute brief rather than the conclusion, then a third seat in a fresh context that rebased the branch onto `main` and reviewed it to land it, each closing what the one before had found | the first seat proved the move byte-identical from git and found the widened check could be narrowed to blindness and five row shapes it did not see; the landing seat ran the widened check against `main`'s own file and found it named fifteen of the sixteen hidden rows, the sixteenth having been written onto another row's physical line where no check was looking; each finding is closed with its mutation re-run and its shape planted in a control; the open holes are named in the record | `2026-09-16-known-issues-rows-in-comment.md` |
| 2026-09-16 | the 2026-09-15 round-one fix panel's scope restated in hashes this gate can read: the row above writes it `a0d8e8a..HEAD`, and a moving reference is not a range end, so six of the commits that panel reviewed were covered by nothing once the gate's own baseline stopped hiding them (KI-035); the register is append-only, so the correction is appended and the original row stands | ffa63f1..07bae16 | no new review: the seats, the reading and the verdict are the row above's | unchanged, and this row adds none of its own. `a0d8e8a` is `ffa63f1` after the 2026-09-16 history rewrite, and HEAD when the original row was written was `07bae16`, the commit that wrote it, so this range names exactly the commits the original meant | `2026-09-15-round-one-fix-panel.md` |
| 2026-09-16 | the second CI run's one failure: the review-register gate's own baseline, which made its reach a property of the clock and the host, and the range resolution that follows a history rewrite (a confirmed defect on `main`, and it touches `tests/gates/`) | 737b615 | in-family, the session that made the change under a bounded brief, working from the failed run's logs and from a fresh clone; not an independent seat, which is this row's weakness | the brief's hypothesis, that pre-rewrite objects left in this machine's store were resolving scopes a clone could not, is refuted: a fresh clone runs the gate green and the failure reproduces only under the runner's timezone. The cause is git's approximate date parser filling a bare date's missing hour from the current clock, so six commits were invisible here and demanded rows there; both new controls were watched red before the fix, and the fixed gate went red on this machine with exactly the six commits CI had named before any row was appended | `2026-09-16-ci-second-run.md` |
| 2026-09-16 | the fifth hook, a log-first PreToolUse and SessionEnd lock that gives one live agent session a checkout and sends a second one to its own worktree (G57), its ten controls, the widened registration gate, and the working-agreement row | 9881bc4 | in-family, the main session; the script was driven by hand outside the repository before a test was written, and every control drives it the way the runtime does, including the remedy itself | log-first with a mode file and a fifteen-minute liveness window; six claims refuted (the matcher entry, the widening, sub-agents, a dead session, speed, falsifiability); registration is the human's edit and the settings gate stays red until it lands | `2026-09-16-one-session-guard.md` |
| 2026-09-16 | registration of the one-session-per-checkout hook in the hook settings (a human edit; the fifth hook, G57) | d738505 | the human ran the generated script; the settings gate proves both entries and hooks_status counts them | the settings test green, five of five scripts registered | `2026-09-16-one-session-guard.md` |
| 2026-09-16 | the first web slice: new read paths on the upsert repository and the digest assembly service (a module started and finished on a review-required surface) | 69d369b | in-family, the session that built it under a bounded brief, tests before the code; not an independent seat, which is this row's weakness | the reads land beside the existing ones with no write path touched, and two existing reads take an upper bound that is a no-op for their current callers and is what makes a report of an older run truthful; three things the brief did not settle are returned to the main session rather than papered over -- a changed settings fingerprint cannot be resolved into the keys that changed, a `RunContext.warn` warning survives only as a count, and the ranked window's `of` is taken from the list shown so the population counted and the population listed cannot disagree | `2026-09-16-plan-v2-deep-review.md` |
| 2026-09-16 | the first web slice pulled forward from M2: new read paths on the upsert repository, the web layer and its perimeter, the digest assembly service, and the working agreement's command list | 69d369b..24fc717 | in-family, the session that built it under three bounded briefs, tests before the code, each brief read against the routing rows it touched; not an independent seat, which is this row's weakness | the slice is read-only by construction and no write path was touched: the routes reach the database only through `services/`, the layers contract was widened so the composition root's own import of `web` is visible to the gate rather than hidden behind an application string uvicorn would import by name, and the rule it used to carry -- `web` never imports `cli` -- is now its own forbidden contract; the Host check is refused at 421 on reads, with a control that serves the same request through the same inner application with the middleware taken away; the digest is a route and never a file, proven on the rendering by a post scrubbed after collection appearing in neither the list nor its denominator; three gaps are recorded rather than papered over (a settings fingerprint that cannot be resolved into the keys that changed, a warning that survives only as a count, and post permalinks stored site-relative, which every digest rendering turns into a link to this server); and the register's own header, which named the review-required surfaces under the pre-rename package path, is corrected by the dated parenthetical the append-only diff allows, a rewrite of that line being refused by the gate rather than by the rule | `2026-09-16-plan-v2-deep-review.md` |
| 2026-09-16 | KI-036, the post links the first web slice recorded as site-relative, fixed on the read that hands them to the digest (a confirmed defect on `main`, and it touches `db/repo.py`) | 0ba61c9 | in-family, the session that made the change under a bounded brief; not an independent seat, which is this row's weakness | the defect was already on the record, in the row above, so this round's work was the shape of the fix rather than its discovery: the host is joined on once at the read instead of in each of the three renderings that take their link from one model, the stored column keeps the relative path the collector wrote (asserted in the same test, so the fix cannot have been bought by rewriting rows), and no helper wraps the one call site. Both tests were watched red against the unfixed read; the digest golden did not move, having carried absolute links all along, which is part of why the defect survived the slice | `2026-09-16-digest-permalinks.md` |
