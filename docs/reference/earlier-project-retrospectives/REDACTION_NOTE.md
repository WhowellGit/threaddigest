# Redaction and disposition note (2026-09-13)

Eight retrospective documents from Wes's earlier project arrived on 2026-09-12 as point-in-time copies of documents that live on in that project's own repository. On 2026-09-13 a tiered assessment found their residual, not-yet-distilled value low (the residuals are distilled in `docs/learnings/LEARNINGS_TRANSFER.md` §7), and Wes chose to keep the three narrative files and move the rest out of the repository.

**Kept here, redacted:** `WHY_THE_GUARDS_EXIST.md`, `CRITICAL_FAILURES_RETROSPECTIVE.md`, `INDEX.md`. Eight substitutions in total, verified by two independent fresh-eyes passes with no residual and no over-redaction: the employer's name where it denoted the company's internal workflow → `[company]`; the internal organization codename → `[org]`; one internal telemetry identifier → `[internal-symbol]`; two internal bot accounts → `[service-account-1]`, `[service-account-2]`. Line counts are unchanged, so citations by line number into these files still hold. Public product names, Wes's own framework and register names, commit hashes, numbers, and academic citations were kept.

**Moved to the archive outside the repository** (`~/repos/insightminer-desktop-archive/Reddit/Database_Key_Learnings/`, byte-identical to the originals): `DEAD_ENDS_AND_RULED_OUT.md`, `KEY_LEARNINGS.md`, `PROJECT_JOURNEY.md`, `SYSTEM_ARCHITECTURE_AND_REBUILD.md`, `VALUE_STAGE_KEY_LEARNINGS.md`. They carry dense internal material (names, logins, tickets, hosts, a Slack channel id, machine names) and were judged not worth redacting. Agents on this machine can still read them there; citations of the form `SAR:2741` in the reviews refer to line numbers in those originals.

**History.** Local git history was rewritten on 2026-09-13 with `git filter-repo` so that no commit carries the eight unredacted files; a verified bundle of the pre-rewrite repository sits in `~/repos/insightminer-desktop-archive/git-bundles/`. Commit hashes cited in documents were remapped from the tool's commit map in the same change.
